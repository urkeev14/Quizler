<resources xmlns:tools="http://schemas.android.com/tools">
       <!-- Base application theme. -->
    <style name="Theme.Quizler" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryLightColor</item>
        <item name="colorOnPrimary">@color/primaryTextColor</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryLightColor</item>
        <item name="colorOnSecondary">@color/secondaryTextColor</item>

        <!-- Background color -->
        <item name="backgroundColor">@color/colorBackground</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Font -->
        <item name="fontFamily">@font/gloria_hallelujah</item>
        <item name="android:fontFamily">@font/gloria_hallelujah</item>

        <!-- Customize your theme here. -->
    </style>

    <!-- TextInputLayout default styling -->
    <style name="Widget.Quizler.TextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="android:layout_marginHorizontal" tools:targetApi="o">@dimen/normal_100</item>
        <item name="android:paddingBottom">@dimen/small_100</item>
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">16dp</item>
        <item name="hintTextAppearance">@style/TextAppearance.Quizler.HintText</item>
    </style>

    <!-- Hint text style-->
    <style name="TextAppearance.Quizler.HintText" parent="TextAppearance.MaterialComponents.Subtitle2">
        <item name="android:textColor">@color/primaryDarkColor</item>
    </style>
</resources>